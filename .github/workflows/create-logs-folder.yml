name: Create Logs Folder

on:
  push:
    branches:
      - main  # 触发条件，当推送到 main 分支时运行
  workflow_dispatch:

jobs:
  create-logs-folder:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Create logs folder
        run: mkdir logs
